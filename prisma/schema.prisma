generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  relationMode = "Prisma"
}
 
model Category {
  id             String @id @default(uuid())
  name           String
}